package api

// TODO: Actually implement "api" tests

/*func TestVersion(t *testing.T) {
	_, err := semver.Make(Version)
	if err != nil {
		t.Errorf("Version failed to validate with error: %s", err)
	}
}*/
